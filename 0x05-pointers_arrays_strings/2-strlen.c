#include "main.h"

/**
 * _strlen - fucntion to get length of string
 * @s: string pointer passed to this function
 * Return: return length of string
 */

int _strlen(char *s)
{
	int len;

	len = 0;
	while (s[u] != '\0')
	{
		len++;
	}
	return (len);
}
