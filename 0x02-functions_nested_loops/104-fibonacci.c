#include <stdio.h>

/**
 * main - finds and prints the first 98 Finonacci numbers
 * starting with 1 and 2
 * followed by new line
 * Return: Always 0 (success)
 */

int main(void)
{
	unsigned long int i, j, k, j1, j2, k1, k2;

	j = 1;
	k = 2;

	printf("%lu. j);
